
jar {
   archiveFileName = "aldorunit.jar"
}

def libFiles = ["foam", "foamj"]

dependencies {
   compile files(libFiles.collect({"../lib/${it}.jar"}))
   compile group: 'junit', name: 'junit', version: '4.12'
}

