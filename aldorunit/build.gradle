
jar {
   archiveFileName = "aldorunit.jar"
}

def libFiles = ["foam", "foamj"]

dependencies {
   implementation files(libFiles.collect({"../lib/${it}.jar"}))
   implementation group: 'junit', name: 'junit', version: '4.12'
}

