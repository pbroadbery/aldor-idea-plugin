language: java
jdk:
  - openjdk8

before_install:
  - echo "wrapper {gradleVersion='4.4.1'}" > nada.gradle
  - cat nada.gradle
  - gradle -b nada.gradle wrapper
  - ./gradlew --version
  - ./gradlew tasks
  
script:
  - ./gradlew test -Daldor.build.skip_ci=true
  