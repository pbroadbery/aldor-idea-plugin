
jar.archiveFileName = "aldor-jps-plugin.jar"

task list(dependsOn: configurations.compile) {
    doLast {
        println "classpath = ${configurations.compile.collect { File file -> file.toString()}}"
    }
}
